extends Node2D


# Called when the node enters the scene tree for the first time.
#func _ready():
#	await get_tree().create_timer(0.15).timeout
#	DialogManager.display_dialog.emit(DialogManager.first_dialog_section, DialogManager.mr_tuto_character)
